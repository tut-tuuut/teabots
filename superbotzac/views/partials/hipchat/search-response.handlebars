{{#if noResult}}
  Désolé aucun résultat ne correspond à votre recherche <strong>{{query}}</strong>
{{else}}
  Voici les {{messagesCount}} {{#if tooManyResults}}meilleurs {{/if}}résultats de votre recherche <strong>{{query}}</strong> :
{{/if}}
<table>
  {{#each messages}}
  <tr>
    <td>{{this.author}} <i>(@{{this.username}})</i></td>
    <td>{{this.date}}</td>
    <td>{{this.message}}</td>
  </tr>
  {{/each}}
</table>
